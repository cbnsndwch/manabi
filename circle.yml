machine:
  hosts:
    dev.manabi.io: 127.0.0.1
  services:
    - redis

dependencies:
  pre:
    - pip install

deployment:
  production:
    branch: master
    commands:
      - cd config
      - pip install -r requirements.txt
      - ansible-playbook
