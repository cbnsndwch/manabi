machine:
  hosts:
    dev.manabi.io: 127.0.0.1
  services:
    - redis

deployment:
  production:
    branch: master
    commands:
      - eval `ssh-agent`
      - ssh-add /home/ubuntu/.ssh/id_circleci_github
      - ssh-add && config/deploy.sh
