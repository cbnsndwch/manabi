machine:
  hosts:
    dev.manabi.io: 127.0.0.1
  services:
    - redis

dependencies:
  pre:
    - pip install

deployment:
  production:
    branch: master
    commands:
      - pip install -r config/requirements.txt
      - ansible-playbook -i config/playbooks/inventory config/playbooks/deploy.yml
