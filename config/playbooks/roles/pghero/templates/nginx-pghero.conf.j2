# {{ ansible_managed }}

server {
    listen 80;

    server_name {{ pghero.hostname }};
    charset     utf-8;

    location / {
        proxy_pass http://localhost:{{ pghero.oauth2_proxy_port }};
    }
}
